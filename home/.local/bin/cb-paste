#!/bin/sh
# Unified clipboard paste — writes system clipboard to stdout.
# Works on macOS (pbpaste) and Linux/X11 (xsel).
case "$(uname)" in
  Darwin) exec pbpaste ;;
  *)      exec xsel --clipboard --output ;;
esac
